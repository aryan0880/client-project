{
  "name": "client-project-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix backend",
    "start": "npm start --prefix backend",
    "seed": "npm run seed --prefix backend"
  }
}
